# Local LLM Brain

CPU-only, production-style local LLM backend with
traceable, explainable outputs.

## Status
- Day 1: API skeleton + logging + session-ready structure

## Tech
- Python
- FastAPI
- CPU-only inference (planned)
